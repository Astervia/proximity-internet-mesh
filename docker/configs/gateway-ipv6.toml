# Gateway node — IPv4 + IPv6 single-hop Docker tests.

[node]
name = "gateway-v6"
data_dir = "/var/lib/pim"

[interface]
mtu = 1400

[transport]
listen_port = 9100

[security]
key_file = "/var/lib/pim/node.key"

[gateway]
enabled = true
nat_interface = "eth1"
