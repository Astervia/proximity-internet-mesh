# Generated by scripts/generate_client_full_config.sh at 2026-04-16T00:57:18Z
# Auto-detected interfaces:
#   wifi_direct.interface  = "wlan0"
#   bluetooth.interface    = "auto"
#
# Review these values before starting the daemon on a real host:
#   ip -br link
#   iw dev
#   bluetoothctl show

[node]
name = "clientbtonly"
data_dir = "/var/lib/pim"

[interface]
name = "pim0"
mesh_ip = "auto"
mtu = 1400

[discovery]
enabled = false
port = 9101
broadcast_interval_ms = 5000
peer_timeout_ms = 30000
connect_relays = true
connect_gateways = true

[transport]
type = "tcp"
listen_port = 9100

[routing]
max_hops = 10
algorithm = "distance-vector"
route_expiry_s = 300

[relay]
enabled = false

[security]
key_file = "/var/lib/pim/node.key"
require_encryption = true

[wifi_direct]

enabled = false
interface = "wlan0"
go_intent = 7
listen_channel = 6
op_channel = 6
connect_method = "pbc"

[bluetooth]

enabled = true
interface = "auto"
radio_discovery_enabled = true
device_name_prefix = "PIM-"
local_alias = "PIM-clientbtonly"
connect_pan = true
serve_nap = false
nap_bridge = "br-bt"
auto_discover_peers = true
poll_interval_ms = 2000
scan_interval_ms = 5000
peer_discovery_interval_ms = 2000
bluetoothctl_timeout_s = 15
discoverable_timeout_s = 180
startup_timeout_ms = 15000

# Optional static peers. Leave these commented if you want to rely entirely on
# auto-discovery and outbound connection attempts.
#
# [[peers]]
# mechanism = "tcp"
# address = "192.168.1.20:9100"
# label = "relay-a"
#
# [[peers]]
# mechanism = "bluetooth"
# ip = "192.168.44.2"
# label = "bt-gateway-a"
