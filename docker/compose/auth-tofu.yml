name: pim-auth-tofu

services:
  gateway:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: pim:latest
    hostname: gateway
    entrypoint:
      - /bin/bash
      - -lc
      - "test -f /var/lib/pim/node.key || printf '\\001%.0s' $(seq 1 32) > /var/lib/pim/node.key; exec /entrypoint.sh"
    volumes:
      - ../configs/auth/tofu-gateway.toml:/etc/pim/pim.toml:ro
      - gateway-data:/var/lib/pim
    cap_add:
      - NET_ADMIN
      - NET_RAW
    devices:
      - /dev/net/tun:/dev/net/tun
    sysctls:
      net.ipv4.ip_forward: "1"
    networks:
      pim-net:
        ipv4_address: 172.43.0.10
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "test -f /run/pim.pid && kill -0 $(cat /run/pim.pid) 2>/dev/null && ip link show pim0 2>/dev/null | grep -q UP",
        ]
      interval: 2s
      timeout: 4s
      retries: 25
      start_period: 5s

  client:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: pim:latest
    hostname: client
    entrypoint:
      - /bin/bash
      - -lc
      - "test -f /var/lib/pim/node.key || printf '\\002%.0s' $(seq 1 32) > /var/lib/pim/node.key; exec /entrypoint.sh"
    volumes:
      - ../configs/auth/tofu-client.toml:/etc/pim/pim.toml:ro
      - client-data:/var/lib/pim
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    depends_on:
      gateway:
        condition: service_healthy
    networks:
      pim-net:
        ipv4_address: 172.43.0.20
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "test -f /run/pim.pid && kill -0 $(cat /run/pim.pid) 2>/dev/null && ip link show pim0 2>/dev/null | grep -q UP",
        ]
      interval: 2s
      timeout: 4s
      retries: 25
      start_period: 8s

networks:
  pim-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.43.0.0/24

volumes:
  gateway-data:
  client-data:
