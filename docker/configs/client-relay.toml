# Client node — connects via single relay (multi-hop path).

[node]
name = "client"
data_dir = "/var/lib/pim"

[interface]
mtu = 1400

[transport]
listen_port = 9100

[security]
key_file = "/var/lib/pim/node.key"

[[peers]]
mechanism = "tcp"
address = "relay:9100"
label = "relay"
